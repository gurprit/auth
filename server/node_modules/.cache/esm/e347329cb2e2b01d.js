let express,mongoose,cors,readdirSync;_b2e‍.w("express",[["default",["express"],function(v){express=v}]]);_b2e‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_b2e‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_b2e‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);



const morgan = require ('morgan');


require ('dotenv').config();

//app
const app = express();

// db
mongoose.connect(process.env.DATABASE, {
    useNewUrlParser: true, 
    useUnifiedTopology: true, 
    useFindandModify: false, 
    useCreateIndex: true, 
})
.then(() => console.log('DB CONNECTED'))
.catch((err) => _b2e‍.g.console.log('DB CONNECTION ERROR ', err))

//middleware
app.use(cors());
app.use(morgan('dev'));

//routes middleware
readdirSync('./routes').map((r) => app.use('/api', require(`./routes/${r}`)));


//routes


const port = process.env.PORT || 8000;
app.listen(port, () => console.log(`Server is running on port ${port}`));