{

"homepage": "http://gurprit.github.io/auth",

"scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build", 
    "dev" : "next dev",  
    "build" : "next build", 
    "start" : "next start", 
},
    
  "dependencies": {
    "bootstrap": "^4.6.0"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
 
}
